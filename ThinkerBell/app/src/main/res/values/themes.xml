<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.ThinkerBell" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/primary2</item>
        <item name="android:statusBarColor">@color/primary1</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Splash Screen -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/primary2</item>
        <item name="android:windowSplashScreenAnimationDuration" tools:targetApi="s">500</item>

        <!-- Activity 전환 Animation 비활성화 -->
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="Theme.ThinkerBell" parent="Base.Theme.ThinkerBell" />

    <!-- Home Category Tab Text Style -->
    <style name="TabTextStyle">
        <item name="android:textSize">16sp</item>
        <item name="fontFamily">@font/pretendard_semi_bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">#BABABA</item>
    </style>

</resources>